
import React from 'react';

const Header = (props) => {
    return (
        <header className="page-header">
            <h1 className="header-msg">React Hook Examples</h1>
        </header>
    );
}

export default Header;